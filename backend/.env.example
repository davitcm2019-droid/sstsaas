NODE_ENV=development
PORT=5000

# Obrigatórias (a API não inicia sem elas)
MONGO_URI=
JWT_SECRET=
CORS_ORIGIN=http://localhost:5173

# Opcionais
# Render/Mongo geralmente exige SSL; se NODE_ENV não for "production", defina como true.
DATABASE_SSL=false
JWT_EXPIRES_IN=24h
BCRYPT_SALT_ROUNDS=10
